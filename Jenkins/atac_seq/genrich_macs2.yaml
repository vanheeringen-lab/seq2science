# tab-separated file
samples: ../../Jenkins/alignment/local_sample.tsv

# pipeline file locations
result_dir: ../../Jenkins_results  # where to store results
genome_dir: ../../Jenkins  # where to look for or download the genomes
fastq_dir: ../../Jenkins/dag_fastqs  # where to look for or download the fastqs

# produce bigwigs and a trackhub?
create_trackhub: False

# produce MultiQC output?
create_qc_report: False

# peak callers (supported peak callers are macs2, and genrich, and hmmratac is not fully supported yet)
peak_caller:
  macs2:
      --shift -100 --extsize 200 --nomodel --keep-dup 1 --buffer-size 10000
  genrich:
      -y -j
