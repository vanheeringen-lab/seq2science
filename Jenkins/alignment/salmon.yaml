# tab-separated file of the samples
samples: samples.tsv

dedup_dir: salmon_dedups


# which aligner to use
aligner:
  salmon:
    index:  # parameters for indexing
      ''
    align:  # parameters for alignment
      ''