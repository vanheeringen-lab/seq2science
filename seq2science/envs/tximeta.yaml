name: tximeta
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  # linked_txome & txi count table
  - conda-forge::r-base=4.0.3
  - bioconda::bioconductor-tximeta=1.6.3  # 1.8.0 is bugged, >=1.8.3 should be fixed again
  # linked_txome
  - conda-forge::r-r.utils=2.10.1
  # txi counts table
  - bioconda::bioconductor-singlecellexperiment=1.10.1
  - conda-forge::r-tidyverse=1.3.0
  - conda-forge::r-readr=1.4.0
