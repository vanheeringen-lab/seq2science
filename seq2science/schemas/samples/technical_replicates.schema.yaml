$schema: "http://json-schema.org/draft-06/schema#"

description: an entry in the sample sheet

properties:
  technical_replicates:
    type: string
    description: All samples with the same replicate name with be concatenated (unless 'technical_replicates' is set to 'False').
