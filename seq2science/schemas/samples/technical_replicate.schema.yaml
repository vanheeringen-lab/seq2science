$schema: "http://json-schema.org/draft-06/schema#"

description: an entry in the sample sheet

properties:
  technical_replicate:
    type: string
    description: All samples with the same replicate name with be concatenated (unless 'technical_replicate' is set to 'False').
