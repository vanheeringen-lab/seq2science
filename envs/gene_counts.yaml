name: gene_counts
channels:
  - bioconda
  - r
  - conda-forge
  - defaults
dependencies:
  # linked_txome & txi count table
  - r::r-base=3.6.2
  - bioconda::bioconductor-tximeta=1.4.0 # needs to be updated to >1.4.2 when updated on conda!
  # linked_txome
  - r::r-r.utils=2.9.2
  # txi counts table
  - bioconda::bioconductor-singlecellexperiment=1.8.0
  - r::r-tidyverse=1.3.0
  - r::r-readr=1.3.1
#  # htseq
#  - bioconda::htseq=0.11.2
